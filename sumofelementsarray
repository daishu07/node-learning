function sumArray(arr) {
    let sum = 0;
    for (var i = 0; i < arr.length; i++) {
        sum += arr[i];
    }
    return sum;
}
var data = [10,13,15,77];
var finalsum = sumArray(data);
console.log(finalsum);